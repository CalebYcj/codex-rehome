{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Native file and folder selection for ReHome workflows",
  "windows": ["main"],
  "permissions": ["dialog:allow-open", "dialog:allow-save"]
}
