{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window capability for ReHome Desktop",
  "windows": ["main"],
  "permissions": ["core:default"]
}
