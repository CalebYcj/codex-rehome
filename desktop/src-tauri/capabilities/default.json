{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ReHome Desktop main window",
  "windows": ["main"],
  "permissions": [
    "core:app:allow-version",
    "updater:default",
    "process:allow-restart"
  ]
}
